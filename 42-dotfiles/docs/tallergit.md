locate file in log

```shell
git log -p -- "Taller Git(hub).pdf"
```

```shell
git checkout 700f845 "Taller Git(hub).pdf"
```
git submodule add git@github.com:srioboo/tallerGit-collab.git
update submodule with 'git submodule update'
